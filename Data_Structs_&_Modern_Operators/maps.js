/*Maps are used in javascript to map values to keys,
data is stored in key values pairs in maps, difference
between maps and objects is that in maps the keys can 
have any type, but in objects the keys are strings*/

const rest = new Map();
rest.set("name", "Classico Italino");
rest.set(1, "Frenzy Italian");
console.log(rest.set(2, "Taimoor Ahmmad"));
